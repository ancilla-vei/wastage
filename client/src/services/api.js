import axios from 'axios';const api=axios.create({baseURL:import.meta.env.VITE_API_BASE_URL||'http://localhost:5000/api'});api.interceptors.request.use(c=>{const t=localStorage.getItem('token');if(t)c.headers.Authorization=`Bearer ${t}`;return c});api.interceptors.response.use(r=>r,async e=>{const o=e.config;if(e.response?.status===401&&!o._retry&&localStorage.getItem('refreshToken')){o._retry=true;const r=await axios.post(`${api.defaults.baseURL}/auth/refresh`,{refreshToken:localStorage.getItem('refreshToken')});localStorage.setItem('token',r.data.data.token);localStorage.setItem('refreshToken',r.data.data.refreshToken);o.headers.Authorization=`Bearer ${r.data.data.token}`;return api(o)}return Promise.reject(e)});export default api;
