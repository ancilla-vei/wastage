import{Button,Chip,Paper,Table,TableBody,TableCell,TableHead,TableRow,Typography}from'@mui/material';import{useEffect,useState}from'react';import{officerService}from'../../services/officerService';export default()=>{const[i,set]=useState([]);useEffect(()=>{officerService.complaints().then(r=>set(r.data.data.items))},[]);return <Paper sx={{p:2}}><Typography variant='h3'>Zone Complaints</Typography><Table><TableHead><TableRow><TableCell>ID</TableCell><TableCell>Severity</TableCell><TableCell>Priority</TableCell><TableCell>Status</TableCell><TableCell>Action</TableCell></TableRow></TableHead><TableBody>{i.map(c=><TableRow key={c._id}><TableCell>{c.complaintId}</TableCell><TableCell>{c.aiAnalysis?.severity}</TableCell><TableCell>{c.priorityScore}</TableCell><TableCell><Chip label={c.status}/></TableCell><TableCell><Button href={`/officer/assign/${c._id}`}>Assign</Button></TableCell></TableRow>)}</TableBody></Table></Paper>}
