import{Chip,Paper,Table,TableBody,TableCell,TableHead,TableRow,Typography}from'@mui/material';import{useEffect,useState}from'react';import{officerService}from'../../services/officerService';import HotspotMap from'../../components/maps/HotspotMap';export default()=>{const[i,set]=useState([]);useEffect(()=>{officerService.hotspots().then(r=>set(r.data.data.items))},[]);return <Paper sx={{p:3}}><Typography variant='h3'>Hotspot Report</Typography><HotspotMap items={i}/><Table><TableHead><TableRow><TableCell>Address</TableCell><TableCell>Complaints</TableCell><TableCell>Risk</TableCell></TableRow></TableHead><TableBody>{i.slice(0,10).map(h=><TableRow key={h._id}><TableCell>{h.address}</TableCell><TableCell>{h.complaintCount}</TableCell><TableCell><Chip label={h.predictedRisk}/></TableCell></TableRow>)}</TableBody></Table></Paper>}
