import{Paper,Typography}from'@mui/material';import{useEffect,useState}from'react';import{adminService}from'../../services/adminService';export default()=>{const[i,set]=useState([]);useEffect(()=>{adminService.complaints().then(r=>set(r.data.data.items))},[]);return <Paper sx={{p:3}}><Typography variant='h3'>All Complaints</Typography>{i.map(c=><Typography key={c._id}>{c.complaintId} • {c.zone?.name} • {c.status}</Typography>)}</Paper>}
