import{Paper,Typography}from'@mui/material';import{useEffect,useState}from'react';import{adminService}from'../../services/adminService';export default()=>{const[i,set]=useState([]);useEffect(()=>{adminService.officers().then(r=>set(r.data.data.items))},[]);return <Paper sx={{p:3}}><Typography variant='h3'>Manage Officers</Typography>{i.map(o=><Typography key={o._id}>{o.name} • {o.employeeId} • {o.zone?.name||'Unassigned'}</Typography>)}</Paper>}
