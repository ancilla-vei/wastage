import{ResponsiveContainer,PieChart,Pie,Cell,BarChart,Bar,XAxis,YAxis,Tooltip,LineChart,Line}from'recharts';import{Paper,Typography}from'@mui/material';import{useEffect,useState}from'react';import{adminService}from'../../services/adminService';export default()=>{const[a,set]=useState({totals:{}});useEffect(()=>{adminService.analytics().then(r=>set(r.data.data))},[]);return <Paper sx={{p:3}}><Typography variant='h3'>Admin Dashboard</Typography><Typography>Total complaints: {a.totals?.complaints||0} • Resolved: {a.totals?.resolved||0} • Citizens: {a.totals?.citizens||0}</Typography><div style={{height:300}}><ResponsiveContainer><BarChart data={a.zones||[]}><XAxis dataKey='_id'/><YAxis/><Tooltip/><Bar dataKey='count' fill='#1B4F72'/></BarChart></ResponsiveContainer></div></Paper>}
