import{Paper,Typography}from'@mui/material';import{useEffect,useState}from'react';import{adminService}from'../../services/adminService';export default()=>{const[i,set]=useState([]);useEffect(()=>{adminService.performance().then(r=>set(r.data.data.items))},[]);return <Paper sx={{p:3}}><Typography variant='h3'>Worker Performance</Typography>{i.map((x,n)=><Typography key={n}>Worker {x._id}: {x.completed}/{x.total} completed</Typography>)}</Paper>}
