import{Paper,Typography}from'@mui/material';import{useEffect,useState}from'react';import{adminService}from'../../services/adminService';import HotspotMap from'../../components/maps/HotspotMap';export default()=>{const[i,set]=useState([]);useEffect(()=>{adminService.hotspots().then(r=>set(r.data.data.items))},[]);return <Paper sx={{p:3}}><Typography variant='h3'>All Hotspots</Typography><HotspotMap items={i}/>{i.map(h=><Typography key={h._id}>{h.zone?.name}: {h.complaintCount} reports ({h.predictedRisk})</Typography>)}</Paper>}
