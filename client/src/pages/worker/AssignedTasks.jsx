import{Button,Card,CardContent,Chip,Stack,Typography}from'@mui/material';import{Link}from'react-router-dom';import{useEffect,useState}from'react';import{workerService}from'../../services/workerService';export default()=>{const[i,set]=useState([]);useEffect(()=>{workerService.tasks().then(r=>set(r.data.data.items))},[]);return <Stack spacing={2}><Typography variant='h3'>Assigned Tasks</Typography>{i.map(t=><Card key={t._id}><CardContent><Typography>{t.complaint?.complaintId}</Typography><Chip label={t.status}/><Button component={Link} to={`/worker/tasks/${t._id}`}>Open</Button></CardContent></Card>)}</Stack>}
