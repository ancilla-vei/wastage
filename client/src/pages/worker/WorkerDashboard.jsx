import{Button,Grid,Typography}from'@mui/material';import{Link}from'react-router-dom';import{useEffect,useState}from'react';import{workerService}from'../../services/workerService';import StatCard from'../../components/common/StatCard';export default()=>{const[s,set]=useState({});useEffect(()=>{workerService.performance().then(r=>set(r.data.data.stats))},[]);return <><Typography variant='h3'>Worker Dashboard</Typography><Grid container spacing={2}>{Object.entries(s).map(([k,v])=><Grid item xs={12} md={4} key={k}><StatCard title={k} value={v}/></Grid>)}</Grid><Button component={Link} to='/worker/tasks'>Assigned Tasks</Button></>}
