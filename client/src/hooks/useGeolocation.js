import{useEffect,useState}from'react';export default function useGeolocation(){const[pos,setPos]=useState(null),[error,setError]=useState(''),[loading,setLoading]=useState(true);useEffect(()=>{if(!navigator.geolocation){setError('Geolocation unavailable');setLoading(false);return}navigator.geolocation.getCurrentPosition(p=>{setPos({lat:p.coords.latitude,lng:p.coords.longitude});setLoading(false)},()=>{setError('Location access is required to report waste.');setLoading(false)},{enableHighAccuracy:true})},[]);return{position:pos,error,loading}}
