import{useEffect,useState}from'react';import api from'../services/api';import useSocket from'./useSocket';export default function useNotifications(){const[items,setItems]=useState([]);const socket=useSocket();const load=async()=>setItems((await api.get('/notifications')).data.data.items);useEffect(()=>{load().catch(()=>{})},[]);useEffect(()=>{if(!socket)return;socket.on('notification:new',n=>setItems(i=>[n,...i]));return()=>socket.off('notification:new')},[socket]);const markRead=async id=>{await api.patch(`/notifications/${id}/read`);setItems(i=>i.map(n=>n._id===id?{...n,isRead:true}:n))};return{items,unread:items.filter(i=>!i.isRead).length,markRead}}
