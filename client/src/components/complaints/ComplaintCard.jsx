import{Card,CardContent,Chip,Typography,Stack,Button}from'@mui/material';import{Link}from'react-router-dom';export default function ComplaintCard({complaint}){return <Card><CardContent><Stack direction='row' justifyContent='space-between'><Typography variant='h6'>{complaint.complaintId}</Typography><Chip label={complaint.status}/></Stack><Typography>{complaint.location?.address}</Typography><Chip label={complaint.aiAnalysis?.severity||complaint.priority} color={complaint.priority==='critical'?'error':'warning'} sx={{mr:1}}/><Chip label={`${complaint.supportCount||0} supports`}/><Button component={Link} to={`/citizen/complaints/${complaint._id}`}>View</Button></CardContent></Card>}
