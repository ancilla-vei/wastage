import{AppBar,Toolbar,Typography,Button,Box}from'@mui/material';import{Link}from'react-router-dom';import{useAuth}from'../../contexts/AuthContext';import NotificationBell from'../notifications/NotificationBell';export default function Navbar(){const{user,logout}=useAuth();return <AppBar position='sticky'><Toolbar><Typography variant='h6' sx={{flexGrow:1,fontWeight:800}}>Mangalore Smart Waste Portal</Typography><Button color='inherit' component={Link} to='/'>Home</Button>{user&&<Button color='inherit' component={Link} to={`/${user.role}`}>Dashboard</Button>}{user&&<NotificationBell/>}{user?<Button color='inherit' onClick={logout}>Logout</Button>:<Box><Button color='inherit' component={Link} to='/login'>Citizen Login</Button><Button color='inherit' component={Link} to='/staff-login'>Staff</Button></Box>}</Toolbar></AppBar>}
