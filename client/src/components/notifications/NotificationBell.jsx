import{Badge,IconButton,Menu,MenuItem,ListItemText}from'@mui/material';import NotificationsIcon from'@mui/icons-material/Notifications';import{useState}from'react';import useNotifications from'../../hooks/useNotifications';export default function NotificationBell(){const[anchor,setAnchor]=useState(null);const{items,unread,markRead}=useNotifications();return <><IconButton color='inherit' onClick={e=>setAnchor(e.currentTarget)}><Badge badgeContent={unread} color='error'><NotificationsIcon/></Badge></IconButton><Menu open={!!anchor} anchorEl={anchor} onClose={()=>setAnchor(null)}>{items.slice(0,10).map(n=><MenuItem key={n._id} onClick={()=>markRead(n._id)}><ListItemText primary={n.title} secondary={n.message}/></MenuItem>)}{!items.length&&<MenuItem>No notifications</MenuItem>}</Menu></>}
