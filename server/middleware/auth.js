const jwt=require('jsonwebtoken'),User=require('../models/User');module.exports=async(req,res,next)=>{try{const h=req.headers.authorization||'';const token=h.startsWith('Bearer ')?h.slice(7):null;if(!token)return res.status(401).json({success:false,message:'Authentication required',data:{},errors:[]});const d=jwt.verify(token,process.env.JWT_SECRET||'dev_secret');const u=await User.findById(d.id).select('-password -refreshToken');if(!u||!u.isActive)return res.status(401).json({success:false,message:'Invalid user',data:{},errors:[]});req.user=u;next()}catch(e){res.status(401).json({success:false,message:'Invalid or expired token',data:{},errors:[e.message]})}};
