const r=require('express').Router(),c=require('../controllers/adminController'),auth=require('../middleware/auth'),{requireRole}=require('../middleware/roleCheck');r.use(auth,requireRole('admin'));r.get('/officers',c.officers);r.post('/officers',c.createOfficer);r.patch('/officers/:id',c.updateOfficer);r.patch('/officers/:id/deactivate',c.deactivateOfficer);r.get('/zones',c.zones);r.post('/zones',c.createZone);r.patch('/zones/:id',c.updateZone);r.get('/complaints',c.complaints);r.get('/analytics',c.analytics);r.get('/hotspots',c.hotspots);r.get('/workers/performance',c.workerPerformance);module.exports=r;
