const r=require('express').Router(),c=require('../controllers/officerController'),auth=require('../middleware/auth'),{requireRole}=require('../middleware/roleCheck');r.use(auth,requireRole('officer'));r.get('/workers',c.workers);r.post('/workers',c.createWorker);r.patch('/workers/:id',c.updateWorker);r.delete('/workers/:id',c.deleteWorker);r.get('/zone/analytics',c.analytics);r.get('/zone/hotspots',c.hotspots);r.get('/zone/complaints',c.zoneComplaints);r.post('/complaints/:id/assign',c.assign);r.post('/complaints/:id/reassign',c.reassign);r.post('/complaints/:id/verify',c.verify);module.exports=r;
