const r=require('express').Router(),c=require('../controllers/workerController'),auth=require('../middleware/auth'),{requireRole}=require('../middleware/roleCheck');r.use(auth,requireRole('worker'));r.get('/tasks',c.tasks);r.get('/tasks/:taskId',c.detail);r.patch('/tasks/:taskId/accept',c.accept);r.patch('/tasks/:taskId/start',c.start);r.post('/tasks/:taskId/complete',c.complete);r.get('/performance',c.performance);module.exports=r;
